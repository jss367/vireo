<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow unsigned code in memory (needed for PyInstaller binary) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow loading unsigned dylibs (PyInstaller bundles Python dylibs) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow outgoing network connections (Flask sidecar on localhost) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow incoming network connections (Flask listens on 127.0.0.1) -->
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- Allow read access to all files (sidecar reads photo paths directly, not via NSOpenPanel) -->
    <key>com.apple.security.files.all</key>
    <true/>

    <!-- Allow access to network volumes (NAS, SMB shares with photos) -->
    <key>com.apple.security.files.network.volumes.read-only</key>
    <true/>
</dict>
</plist>
